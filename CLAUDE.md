# Git Reward

깃허브 기여 기록을 RPG 캐릭터 카드로 바꿔 디스코드에 보여주는 서비스.
해커톤 프로젝트이며, 작성자는 실무 프로젝트 경험이 적다. **동작 확인이 항상 최우선이다.**

## 참조 문서

- 프로젝트 구조·데이터 형태·범위는 @docs/기획.md 를 따른다. 임의로 변경하지 않는다.
- 에러가 반복되거나 원인을 찾기 어려우면 @docs/함정.md 를 먼저 확인한다.
  새로 겪은 문제와 해결책은 이 파일에 한 줄로 추가한다. **겪은 것만 적는다.**

## 절대 금지

- **데이터 형태 변경 금지.** 키 이름·타입을 바꾸지 않는다.
  `{"login": str, "avatar": str, "commits": int, "additions": int, "deletions": int, "active_weeks": int}`
- **파일 구조 변경 금지.** `config.py` / `fetch.py` / `stats.py` / `card.py` / `bot.py` / `web.py`
  파일을 쪼개거나 합치거나 새로 만들지 않는다. 필요하면 먼저 물어본다.
- **새 라이브러리 설치 금지.** 사용 중: `requests`, `Pillow`, `python-dotenv`, `discord.py`, `Flask`
  다른 것이 필요하면 먼저 물어본다. 정말 필요하면 이 목록과 `requirements.txt`를 함께 수정하도록 제안한다.
  **설치(`pip install`)는 직접 실행하지 않고 사용자에게 맡긴다. <설치 가이드는 제시를 해준다.>**
- **`.env` 파일을 읽거나 출력하지 않는다.** 값은 `os.getenv()`로만 접근한다.
- 아래는 이 프로젝트에서 만들지 않는다: 웹훅, 게임 시스템(던전, 스테이지 조건 달성형 수집 카드 발급 포함), 배포 설정, 테스트 코드.
  이 게임의 "수집용 카드"는 이 프로젝트의 능력치 카드(`card.py` 출력)와 **다른 것**이니 헷갈리지 않는다.
- **로그인·데이터베이스는 2026-08-21부로 범위에 들어왔다** (스텟 배분을 카드 주인만 하게 막기 위해).
  **DB는 SQLite로 확정**(표준 라이브러리 `sqlite3`, 설치 불필요). 로그인 방식은 아직 미정 —
  담당 팀원이 정하기 전까지 임의로 구현하지 않는다. 자세한 내용은 @docs/기획.md 8번 참고.

## 작업 방식

- **한 번에 한 파일만 수정한다.** 다른 파일은 읽기만 한다.
- 요청받지 않은 리팩터링·구조 개선·성능 최적화를 하지 않는다.
  **동작하는 코드는 건드리지 않는다.**
- 기능을 먼저 완성한 뒤 다듬는다. 처음부터 예외 처리를 촘촘히 넣지 않는다.
- 에러를 `try/except: pass`로 덮지 않는다. 원인을 찾아 알린다.
- 파일을 수정한 뒤에는 **터미널에서 실행해 확인하는 방법**을 함께 알려준다.
  (예: `python fetch.py il-yeon-glitch/askme-project` → `cache/` 에 JSON 생성 확인)

## 코드 스타일

- 주석은 한글로, 초보자가 읽을 수 있게 **무엇을 왜 하는지** 위주로 쓴다.
- 타입 힌트·데코레이터·클래스 없이 단순한 함수로 작성한다.
- 설정값(능력치 이름, 계수, 색상, 크기)은 **반드시 `config.py`에** 둔다.
  다른 파일에 숫자나 색상을 직접 적지 않는다.

## 설명 방식

- 코드를 준 뒤, 핵심 부분이 무슨 일을 하는지 2~3줄로 설명한다.
- 낯선 개념(비동기, 데코레이터 등)이 처음 등장하면 짧게 짚고 넘어간다.
- 여러 방법이 있으면 나열하지 말고 **하나를 골라 이유와 함께** 제시한다.

## 알려진 함정

- `/stats/contributors` 는 첫 호출에서 **202** 와 빈 응답을 반환한다. 재시도 필수.
- 디스코드 슬래시 명령은 **길드 한정 sync**(`guild=`)로 등록한다. 전역 등록은 반영이 느리다.
- 디스코드는 명령 응답이 3초를 넘기면 실패 처리한다. `await interaction.response.defer()` 를 먼저 호출한다.
- 이미지를 `BytesIO` 로 보낼 때 `buf.seek(0)` 을 빠뜨리면 빈 파일이 전송된다.
- Pillow 에서 한글 폰트를 지정하지 않으면 글자가 네모로 깨진다.
