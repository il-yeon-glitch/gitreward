# 내일 할 일

> 이 파일은 임시 메모야. 읽고 나서 지워도 됨 (`docs/`에 넣는 영구 문서 아님).

## 1. 급한 것 — 문서는 바뀌었는데 코드는 아직 옛날 방식

오늘 `기획.md` 6번을 새 레벨링 방식(커밋+추가+삭제 → 레벨 하나로 합산, 레벨업마다 HP/ATK/DEF 성장)으로 바꿨는데, **실제 코드는 아직 안 건드렸어.** 지금 코드는 여전히 옛날 방식(ATK/DEF/AGI/STA 4개, 커밋·추가·삭제·활동주수를 각각 1:1로 환산)으로 동작해. 게임 시스템 담당 팀원이 레벨 공식(계수·`LEVEL_DIVISOR`, `active_weeks` 포함 여부, 경험치 증가폭)을 확정하면 아래 순서로 반영해야 함:

- **`config.py`** (8~26줄) — `STAT_NAMES` / `STAT_WEIGHTS` / `LEVEL_DIVISOR` / `STAT_MAX` 를 새 값으로 교체. 레벨업당 HP/ATK/DEF 성장치, 필요 경험치 증가 공식도 여기 추가.
- **`stats.py`** — `calc_stats()` / `calc_level()` 을 "레벨 → HP/ATK/DEF 성장" 방식으로 다시 작성.
- ~~**`card.py`** — 능력치 라벨을 HP/ATK/DEF 3개로, 카드 레이아웃도 같이 손보기~~ → **2026-08-21 완료.** 막대와 포지션은 아예 없애고 이름·숫자만 크게 남겼다.

## 2. 스텟 배분 명령 (`/능력치`) — 아직 안 만듦

오늘은 **계정 연결(로그인)까지만** 만들었어. 실제로 레벨업 포인트를 쓰는 명령은 없음. 위 1번(레벨 공식)이 먼저 정해져야 만들 수 있음.

- 만들 때 제일 먼저 `db.get_linked_login(interaction.user.id)` 로 "명령 친 사람 = 카드 진짜 주인"인지부터 확인 (`db.py`에 이미 구현돼 있으니 그대로 갖다 쓰면 됨)
- 얘기했던 UI 예시: "현재 12포인트 사용 가능! 체력 | 공격력 | 방어력" — 버튼/셀렉트 메뉴로 배분

## 3. 기타 미정 사항 (`기획.md` 8번, 확장 예정)

- ~~카드 결투(`/vs/<a>/<b>`) 승패 규칙~~ → **2026-08-21 기획에서 제외.** 라우트도 지웠다.
- `active_weeks`(활동 주 수)를 레벨 공식에 넣을지 여부 (담당 팀원 확정 대기)

## 4. 오늘 만든 로그인 기능 — 팀 공유할 것

- GitHub OAuth Client ID/Secret은 `DISCORD_TOKEN`처럼 **담당자 한 명만** `.env`로 보유. 다른 사람한테 카톡/디스코드로 안 보내기.
- `gitreward.db`(계정 연결 정보)는 `.gitignore`에 이미 등록돼 있어 커밋 안 됨. 각자 로컬에서 `/연결`로 새로 만들면 됨.
- `web.py` / `bot.py` 둘 다 켜져 있어야 `/연결`이 동작함 (한쪽만 켜면 링크가 안 열리거나 콜백이 안 됨).

## 5. `discord_id`가 "인증번호" — 다른 게임·스텟 기능 연결할 때 참고

`linked_accounts` 테이블에서 그 사람을 유일하게 가리키는 값은 `discord_id`다 (예: `361413317878611968`). 디스코드 계정이 살아있는 한 안 바뀌는 값이라, 나중에 다른 기능(스텟 배분, 다른 팀원의 게임)과 연결할 때도 이 값을 공통 식별자로 쓰면 된다.

| 컬럼 | 뜻 | 역할 |
|---|---|---|
| `discord_id` | 디스코드 고유 ID | **그 사람을 유일하게 가리키는 값(인증번호 역할)** |
| `github_login` | 연결된 GitHub 아이디 | 카드 데이터(`{"login": ...}`)와 매칭시키는 값 |
| `linked_at` | 연결된 시각 | 기록용, 식별용 아님 |

- OAuth access_token은 저장 안 함(로그인 이름만 확인하고 바로 버림) — 그래서 별도 세션 토큰이 없고 `discord_id` 자체가 식별자 역할을 함
- 조회는 `db.get_linked_login(discord_id)` 하나로 끝남. 다른 기능에서도 이 함수를 그대로 재사용하면 됨
- 다른 팀원의 게임(수집용 카드 시스템)이 별개 프로젝트라면, `discord_id`를 공통 키로 쓰는 것까진 맞는 방향인데 — 그쪽이 `gitreward.db`를 직접 읽을지, 우리가 조회용 함수/API를 따로 내줄지는 그때 담당 팀원과 정할 것
